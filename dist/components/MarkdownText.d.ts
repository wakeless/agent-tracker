import React from 'react';
interface MarkdownTextProps {
    children: string;
}
/**
 * Renders markdown text in the terminal using marked and marked-terminal.
 * Converts markdown to ANSI formatted text that Ink can display.
 */
export declare function MarkdownText({ children }: MarkdownTextProps): React.JSX.Element;
export {};
//# sourceMappingURL=MarkdownText.d.ts.map