{
  "name": "agent-tracker-monorepo",
  "version": "0.0.3",
  "private": true,
  "description": "Monorepo for Agent Tracker - TUI and Web dashboard for tracking Claude Code sessions",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/tui",
    "packages/web"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "npm run dev -w @agent-tracker/tui",
    "dev:web": "npm run dev -w @agent-tracker/web",
    "build": "npm run build -w @agent-tracker/core && npm run build -w @agent-tracker/tui && npm run build:mcp",
    "build:core": "npm run build -w @agent-tracker/core",
    "build:tui": "npm run build -w @agent-tracker/tui",
    "build:web": "npm run build -w @agent-tracker/web",
    "build:mcp": "cd mcp && tsc",
    "start": "npm run start -w @agent-tracker/tui",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "demo:session": "./scripts/test-session.sh",
    "validate:work-summary": "tsx scripts/validate-work-summary.ts",
    "debug:sessions": "tsx scripts/debug-sessions.ts",
    "debug:timeline": "tsx scripts/debug-timeline.ts",
    "docs:dev": "cd website && npm start",
    "docs:build": "cd website && npm run build",
    "docs:serve": "cd website && npm run serve",
    "clean": "rm -rf packages/*/dist node_modules/.cache"
  },
  "keywords": [
    "cli",
    "tui",
    "ink",
    "typescript",
    "claude",
    "claude-code",
    "session-tracking",
    "monitoring",
    "dashboard"
  ],
  "author": "Agent Tracker Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wakeless/agent-tracker.git"
  },
  "bugs": {
    "url": "https://github.com/wakeless/agent-tracker/issues"
  },
  "homepage": "https://github.com/wakeless/agent-tracker#readme",
  "devDependencies": {
    "@types/node": "^24.7.2",
    "@types/react": "^19.2.2",
    "@vitest/ui": "^3.2.4",
    "ink-testing-library": "^4.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1",
    "ink": "^6.3.1",
    "ink-markdown": "^1.0.4",
    "marked": "^15.0.12",
    "marked-terminal": "^7.3.0",
    "react": "^19.2.0"
  },
  "overrides": {
    "string-width": "^8.1.0"
  }
}
